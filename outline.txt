======
Outiline
=======

chaoter 1
-we meet nick, tom, daisy and Jorday
- call from Tom
- adding more to outline
move outline and split it

chapter 2 starts 12

git switch -c `name of branch`


git branch -m `new name of branch`
git branch -d `name of branch`
git branch -v -> more infromation about the branches (hash and message of last commit)

git commit -a -m 'message'



fast-forward merge -> applies changes from `merge from` branch without conflict (catches up on changes)